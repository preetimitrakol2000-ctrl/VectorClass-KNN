# VectorClass-KNN

A vector geometry classification system written entirely in Python. By computing multi-dimensional matrix distances from absolute scratch, this library demonstrates a deep grasp of distance-based parametric model mapping.

## 🛠️ Features
* **Custom Distance Frameworks:** Configurable geometric operations.
* **Zero Libraries:** Built entirely without `scikit-learn` or `numpy` imports.
